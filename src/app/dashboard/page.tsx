import { AppShell } from "@/components/app-shell";
import { ArrowUpRight, PackageCheck, ShoppingBag, Wallet } from "lucide-react";
const stats=[["Faturamento do mês","R$ 0,00",Wallet],["Lucro estimado","R$ 0,00",ArrowUpRight],["Pedidos em produção","0",ShoppingBag],["Estoque baixo","1",PackageCheck]] as const;
export default function Dashboard(){return <AppShell title="Visão geral"><div className="mb-6 rounded-2xl bg-ink p-6 text-white"><p className="text-sm font-bold text-lime">BEM-VINDO AO NO CORRE HUB</p><h2 className="mt-1 text-2xl font-black">Controle sua marca. Mantenha o corre.</h2><p className="mt-2 max-w-2xl text-sm text-white/65">Comece cadastrando produtos, insumos e pedidos. Os números do painel serão atualizados automaticamente com o Supabase conectado.</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(([label,value,Icon])=><div className="card" key={label}><div className="flex items-center justify-between"><p className="label">{label}</p><Icon size={18}/></div><p className="mt-4 text-2xl font-black">{value}</p><p className="mt-2 text-xs text-black/50">Aguardando dados reais</p></div>)}</div><div className="mt-6 grid gap-6 lg:grid-cols-2"><div className="card"><p className="font-bold">Fluxo de produção</p><div className="mt-5 flex flex-wrap gap-2">{["Corte","Costura","DTF","Revisão","Embalagem","Entrega"].map((s,i)=><div key={s} className="flex items-center gap-2"><span className="rounded-full bg-black/5 px-3 py-2 text-sm font-semibold">{i+1}. {s}</span>{i<5&&<span>→</span>}</div>)}</div></div><div className="card"><p className="font-bold">Ação recomendada</p><p className="mt-2 text-sm text-black/60">Cadastre seu primeiro insumo de tecido e defina o custo por kg para ativar a formação de preço.</p><a className="action mt-4 inline-block" href="/estoque">Ir para estoque</a></div></div></AppShell>}
